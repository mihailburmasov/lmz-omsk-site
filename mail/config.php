<?php
/**
 * Единая точка настройки почты и антиспама для формы обратной связи.
 * Синхронизируйте контактные данные вручную с assets/js/config.js — общего
 * рантайма у фронтенда и бэкенда нет, поэтому оба файла редактируются отдельно.
 */

return [

    // ---------- Получатель заявок ----------
    // TODO_EMAIL: e-mail, куда будут приходить заявки с сайта. Клиент пришлёт адрес позже.
    'to_email' => '',
    'to_name'  => 'ООО «ЛМЗ»',

    // Адрес и имя отправителя писем (обычно почтовый ящик на том же домене, что и сайт).
    // TODO: завести почтовый ящик на хостинге и указать его здесь после того, как будет известен домен.
    'from_email' => 'noreply@example.com',
    'from_name'  => 'Сайт ООО «ЛМЗ»',

    // ---------- SMTP ----------
    // TODO: заполнить реальными данными SMTP-сервера хостинга/почты клиента.
    'smtp' => [
        'enabled'    => true,      // false — временно отправлять через встроенную функцию mail() (для отладки)
        'host'       => 'smtp.example.com',
        'port'       => 587,       // 587 — STARTTLS, 465 — SSL/TLS
        'encryption' => 'tls',     // 'tls' или 'ssl'
        'username'   => 'noreply@example.com',
        'password'   => 'TODO_SMTP_PASSWORD',
        'debug'      => 0,         // 0 — выключено, 2 — подробный лог в error_log (для отладки проблем с отправкой)
    ],

    // ---------- Телефон компании (для письма-уведомления) ----------
    'company_phone' => '+7 991 380-06-44',

    // ---------- Антиспам ----------
    'antispam' => [
        'min_fill_seconds' => 3,     // форма, заполненная быстрее чем за N секунд, считается ботом
        'max_per_ip_per_hour' => 5,  // лимит отправок с одного IP в час
        'allowed_origins' => [
            // TODO: указать домен(ы) сайта после размещения на хостинге, например:
            // 'https://lmz-omsk.ru',
            // 'https://www.lmz-omsk.ru',
        ],
    ],

    // ---------- Вложения ----------
    'attachment' => [
        'max_size' => 10 * 1024 * 1024, // 10 МБ
        'allowed_ext' => ['pdf', 'jpg', 'jpeg', 'png', 'dwg', 'zip'],
        'allowed_mime' => [
            'application/pdf',
            'image/jpeg',
            'image/png',
            'application/zip',
            'application/x-zip-compressed',
            'image/vnd.dwg',
            'application/acad',
            'application/octet-stream', // многие .dwg определяются сервером именно так
        ],
    ],

    // ---------- Часовой пояс для отметки времени в письме ----------
    'timezone' => 'Asia/Omsk',
];
